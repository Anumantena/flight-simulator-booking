import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import FlightSimBooking from './components/FlightSimBooking'

ReactDOM.render(
  <React.StrictMode>
    <FlightSimBooking />
  </React.StrictMode>,
  document.getElementById('root')
)
